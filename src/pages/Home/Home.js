const Home = () => {
    console.log(process.env.REACT_APP_API_KEY )
    return ( 
        <div>Hello, the home page is not finished yet. You can try searching for books using Searchbar</div>
     );
}
 
export default Home;